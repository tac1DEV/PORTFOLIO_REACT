import { SectionWrapper } from '../atom/SectionWrapper';
import { Draw } from './Draw';

export const DrawSection = () => {
  return (
    <SectionWrapper>
      <Draw />
    </SectionWrapper>
  );
};
